apiVersion: v1
kind: Service
metadata: 
  name: blockchain-service-http
  namespace: blockchain
spec: 
  selector: 
    app: blockchain-uniminuto
  ports:
  - protocol: TCP
    port: 3001
    nodePort: 30001
    targetPort: 3001
    name: svcserver
  type: NodePort
